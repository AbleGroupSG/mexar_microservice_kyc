# Docker Compose Variables
# Copy to .env.docker and configure for your environment
#
# Usage:
#   Local:  docker compose --env-file .env.docker up -d
#   Prod:   docker compose -f docker-compose.prod.yml --env-file .env.docker up -d

# ===================
# MySQL Configuration
# ===================
DB_ROOT_PASSWORD=root-password-change-me
DB_DATABASE=mexar_kyc_msa

# ===================
# Docker Image (Production only)
# ===================
# Docker Hub image path (e.g., username/mexar-kyc or org/mexar-kyc)
DOCKER_IMAGE=your-dockerhub-username/mexar-kyc
IMAGE_TAG=latest

# ===================
# Caddy / Domain Configuration
# ===================
# Local development: kyc.local (uses internal self-signed TLS)
# Production: your-domain.com (uses Let's Encrypt auto HTTPS)
PUBLIC_DOMAIN=kyc.local

# Required for Let's Encrypt certificate (production only)
# This email receives certificate expiration notices
WEBMASTER_EMAIL=admin@example.com
